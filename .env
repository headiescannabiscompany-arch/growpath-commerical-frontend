# Do not set EXPO_PUBLIC_API_URL in the base env file.
# Local development uses .env.development; production builds must provide
# EXPO_PUBLIC_API_URL=https://api.growpathai.com from the build environment.
