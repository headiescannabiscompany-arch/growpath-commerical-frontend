# Default API endpoint consumed by Expo/React Native during acceptance tests.
EXPO_PUBLIC_API_URL=http://127.0.0.1:5001

# Uncomment this line if you keep a backend inside the UI repo.
#BACKEND_DIR=backend

# Default: use the sibling growpath-backend repo.
BACKEND_DIR=../growpath-backend

# Override the backend start command if needed (defaults to `npm run dev`).
#BACKEND_START_COMMAND=npm run dev

# Acceptance runner defaults to the Node acceptance tests; override for Expo/Detox/etc.
#ACCEPTANCE_COMMANDS=expo start --dev-client
