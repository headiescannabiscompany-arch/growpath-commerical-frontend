import React from "react";
import { PlannedScreen } from "@/components/PlannedScreen";

export default function FacilitySopRunsPresetsPlanned() {
  return (
    <PlannedScreen
      title="SOP Presets (Planned)"
      message="SOP presets are planned for a future release."
    />
  );
}
