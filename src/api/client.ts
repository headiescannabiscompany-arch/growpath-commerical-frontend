// Thin TypeScript wrapper around the canonical JS client.
// Keep this file free of implementation logic to avoid duplicate declarations
// and to ensure Jest/Babel parses it consistently.
export * from "./client.js";
export { default } from "./client.js";
